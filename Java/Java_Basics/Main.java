package Java.Java_Basics;

public class Main {
    public static void main(String[] args) {
        System.out.println("Hello, Java!");
    }
}

/*
 * Complete Decoding Of Line No. 2
 * 
 * (1) public:- The public is an access modifier that can be used to specify who
 * can access this main() method.
 * The JVM calls the main() method outside the class. Therefore it is necessary
 * to make the java main() method as public.
 * 
 * (2) static:- static is a keyword in java. we can call this method without
 * creating an instance of the class.
 * JVM calls the main() method without creating an instance of the class,
 * therefore it is necessary to make the
 * java main() method static.
 * 
 * (3) void:- void is a return type of method. The java main() method doesnâ€™t
 * return any value.
 * 
 * (4) main:- main is the name of the method. It is a method where program
 * execution starts.
 * 
 * (5) String args[]:- String in java is a class that is used to work on Strings
 * and args is a reference variable that
 * refers to an array of type String. If you want to pass the argument through
 * the command line then it is necessary to
 * make the argument of the main() method as String args[].
 * 
 */
